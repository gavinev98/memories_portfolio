import React from 'react';

import useStyles from './styles';

const Form = () => {

     //calling styles class.
     const classes = useStyles();


    return (
        <h1> Form </h1>
    );
};

export default Form;